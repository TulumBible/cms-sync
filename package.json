{
  "name": "cms-sync",
  "version": "0.2.0",
  "private": true,
  "description": "Convex → JSON sync for the Core hospitality network (tulumbible.com, future cabosbible.com, etc). Polls events-management's /cms-snapshot/* endpoints and commits per-city JSON files for downstream consumption by Astro, tb-ai-concierge, and tulum-core.",
  "type": "module",
  "scripts": {
    "sync": "tsx --env-file-if-exists=.env.local src/sync.ts",
    "type-check": "tsc --noEmit",
    "test": "node --import tsx --test src/lib/*.test.ts src/convex/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22.7"
  }
}
