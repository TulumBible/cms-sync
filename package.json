{
  "name": "tulum-cms-sync",
  "version": "0.2.0",
  "private": true,
  "description": "Convex → JSON sync for tulumbible.com. Polls events-management's /cms-snapshot/* endpoints and commits per-city JSON files for downstream consumption by Astro, tb-ai-concierge, and tulum-core.",
  "type": "module",
  "scripts": {
    "sync": "tsx src/sync.ts",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
